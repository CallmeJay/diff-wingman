# v0.3 验收记录

日期：2026-09-22。使用临时合成 Git 仓库和第二版的合成快照，没有对业务仓库执行模型调用或测试。

| 范围         | 已验证结果                                                                                                                                                                                      |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 旧行为       | Git 版本、暂存区、工作区、导读、笔记和原有人工状态的定向测试继续通过；工作区快照仍只读。                                                                                                        |
| 静态引用     | TypeScript Language Service 在冻结的 JS/TS 文本中定位别名调用，排除无关同名函数；真实 Git 快照的引用绑定到变更并标注运行时待核对。静态引用使用独立 ID，旧快照补充引用时保留原导读、笔记及状态。 |
| 逐条人工判断 | HTTP 测试验证依据必填、未知判断拒绝、导读改变后旧记录待重核、工作区源码改变后拒绝继续确认。                                                                                                     |
| 报告         | HTTP 测试验证需求、变更、源码位置、人工依据、未审查与过期标记；报告不声称自动审查或测试通过。                                                                                                   |
| 浏览器       | 第二版快照可打开；逐条“有疑问”保存后刷新仍存在；报告按钮显示可复制的 Markdown 预览；同一源码重新捕获后显示跨文件静态引用且旧人工状态保留。                                                      |

13 项定向测试通过，`pnpm build` 通过。没有执行完整 `pnpm test`，没有 commit、push 或公共部署。浏览器内 `.md` 文件的下载事件未能由测试环境捕获；Markdown 预览和服务端报告内容已验证。未做大型业务仓库的性能评测，也未验证运行时调用链。
