# v0.4 验收记录

日期：2026-09-22。使用临时合成 Git 仓库验证第四版；没有在业务仓库运行脚本。

| 范围 | 结果 |
| --- | --- |
| 固定源码 | 测试先改变目标仓库工作区，再从目标 commit 还原源码；还原内容仍是 commit 中的 `finally` 版本，目标仓库 `git status` 前后相同。暂存区/工作区模式被拒绝，符号链接被拒绝。 |
| 命令边界 | 仅从目标 commit 的 `package.json` 读取检查脚本；`deploy` 脚本不会出现在清单中。Docker 参数明确关闭网络、只读挂载源码，限制权限、进程、内存和 CPU；npm 隐式前后置脚本被关闭。 |
| 证据与失败 | HTTP 测试验证用户选定的判断、脚本、触发条件和预期结果；退出码 1 与日志保存到同一快照和报告，不改变人工判断。导读改变后报告标记旧关联待重核；隔离环境不可用时返回 503，不回退到宿主机。 |
| 资源边界 | 子进程测试验证标准输出超过 32 KiB 时截断，超时后停止。 |
| 旧行为 | 第二版 2 项、第三版 3 项、服务端旧流程 1 项定向测试通过；第四版 5 项定向测试通过；`pnpm build` 通过。 |
| 页面 | 在本地 127.0.0.1:4318 打开已有 commit 快照，确认“可复现验证”清单出现；Docker 尚未启动时明确提示不可用，旧导读、人工记录和报告入口仍可见。 |
| 真实 Docker 容器 | 在 Docker Desktop 29.6.2 中运行 `pnpm check:v4:docker`：通过第四版 HTTP 接口选中目标 commit 的 `test:boundary`，实际容器返回 0，并输出 `BOUNDARY_OK`。容器读到目标 commit 的 `finally` 源码，而非后来改动的工作区；写入 `/workspace` 被拒绝，连接外网地址返回网络不可达，隐式 `pretest` 没有执行。第二个 `test:fail` 实际返回 7，标准错误被保存到报告；人工判断仍未审查。临时仓库执行前后 `git status` 相同，验收后无遗留 `review-verify` 容器。 |

本次使用官方 `node:22-alpine`，本地镜像 ID 为 `sha256:b6f26b36c8ff49624cfdac716b8ea1138d606df02586a77d364bb5536a634f85`。Docker Desktop 已启动，镜像已下载到本机。真实容器验收只使用合成仓库；仍未验证任何业务项目的依赖能否在该镜像中运行。没有执行完整 `pnpm test`，没有 commit、push 或公共部署。
