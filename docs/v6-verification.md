# v0.0.6 验收记录

验证日期：2026-09-23。工作区：`main`，开发基线 `fbb02e9`。本记录覆盖 v0.0.6 改动；未执行发布、push 或真实 GitLab 写入。

## 自动验证

| 项目 | 结果 | 证据范围 |
| --- | --- | --- |
| `pnpm build` | 通过 | TypeScript 检查、Vite Web 构建和服务端编译 |
| `node --import tsx --test tests/v6-review-core.test.ts tests/v5-gitlab.test.ts tests/server.test.ts tests/web-diff.test.ts` | 通过 | 8 项测试：100 文件筛选和状态统计、指纹失效、文件状态/位置/评论 API、旧版 GitLab 与服务端/diff 回归 |
| `git diff --check` | 通过 | 改动文件无空白错误 |

按项目规则，本次未准备 push，未运行 `pnpm test`。自动测试没有替代桌面安装包、真实 GitLab MR 或完整人工审查验收。

## 真实页面验证

使用本机服务和临时 Git 仓库，创建包含 5 个变更文件的 commit 快照，并以浏览器操作页面：

- 两个相距 70 行的 hunk 都能跳转，原始范围分别显示为 `@@ -7,7 +7,7 @@`、`@@ -77,7 +77,7 @@`；中间 63 行折叠，向上扩展后剩余 43 行，真实变更没有被隐藏。
- 文件状态、未审查计数、下一个未审查文件、有评论筛选、文件搜索及树形视图使用同一份快照记录；搜索条件在刷新后保留，搜索无结果后清空条件能恢复文件与 diff。
- 在第二个 hunk 阅读后，持久化的 `readingPosition` 指向对应文件、修改后第 77 行和第二个 hunk；刷新后页面恢复到该 hunk。
- 本地评论能保存并显示在对应文件；输入框聚焦时 Alt+R 不会标记文件，离开输入框后快捷键生效。
- 工作区快照创建后再次改变源码，刷新页面会显示“待重核 · 原已审查”，并禁用当前文件审查状态操作。

## 保留行为和边界

- 旧版 GitLab 单行草稿的创建、编辑、删除和导出由现有回归测试覆盖；v0.0.6 的本地评论使用独立字段，未改动 GitLab 草稿数据结构。
- 既有服务端和 diff 测试通过；本次未执行 Codex 导读、Docker 容器、真实 GitLab MR、Electron 桌面打包或 100 文件浏览器交互压测。
- 提交版本快照不可变；暂存区、工作区或 GitLab MR 变旧时，旧人工状态留在原快照中供查看，不能作为当前源码已审查的证明。评论不发布到代码托管平台。
