# v0.0.7 验收记录

验证日期：2026-09-23。工作区：`main`，开发基线 `62f8e8f`。本记录覆盖 v0.0.7 增量复审与评论闭环；未执行 push 或发布。

## 自动验证

| 项目 | 结果 | 证据范围 |
| --- | --- | --- |
| `pnpm check:v7` | 通过，3 项 | 重复 hunk 不继承；单 hunk 改动后精确继承及评论位移；临时 Git 仓库和模拟 GitLab 读取器验证导入、编辑、导出、边界失败与旧记录不变 |
| `pnpm check:v5` | 通过，2 项 | 旧版 MR 只读导入、草稿和版本过期行为 |
| `pnpm check:v6` | 通过，2 项 | 文件状态、阅读位置与本地评论的原有行为 |
| `pnpm build` | 通过 | Web 与服务端 TypeScript 检查、Vite 构建；保留原有大 chunk 提示 |
| `git diff --check` | 通过 | 改动文件无空白错误 |

项目规则要求只在准备 push 前运行 `pnpm test`；本次未准备 push，因此未运行完整测试。

## 浏览器验收

在独立端口 `4319` 和临时 Git 仓库、临时数据目录中操作真实页面。确认 v0.0.7 标识、MR 上次审查版本选择框和本地评论编辑表单。保存文件级评论后，总览显示 1 条、未解决 1 条；保存单行修改建议后，总览显示 2 条，Monaco diff 行出现评论标记。把该建议设为已解决后，未解决数降为 1，行标记和评论详情同步显示已解决。保存 1–2 行范围评论后，总览显示 3 条、未解决 2 条及对应范围。

验收用浏览器页、端口 `4319` 服务、临时仓库和数据目录已清理。原有端口 `4318` 预览和用户审查记录未改动。

## 已验证边界与未验证项

- 文件级旧记录只在两侧内容和模式完全相同时继承；v0.0.6 文件状态不会被倒推成旧版 hunk 状态。重复内容或无法唯一对应的变更块不迁移状态或评论。
- 新快照中的评论位置仅在唯一匹配的原变更块增删内容不变且目标行仍有效时自动迁移；无法证明的位置标为待重新定位。旧快照记录不变。
- 模拟 GitLab 读取器没有发出平台写请求；现有 v0.0.5 回归测试覆盖只读客户端。真实私有 GitLab MR、Electron 打包、跨平台行为和 100 文件浏览器负载未验证。
