# v0.0.8 验收记录

验证日期：2026-09-23。开发基线：`main` 的 `c0e23c6`。本记录覆盖本地实现与提交前验证，不代表已 push 或发布。

## 已验证

- `pnpm build` 通过。Vite 提示主包超过 500 KB；该提示并非构建失败，打包优化不属于本次范围。
- `pnpm check:v8` 的 5 个定向场景通过：逐块证据来源与旧状态隔离、固定 Git 树调用/数据关系、OID 回跳、同名局部变量与函数重排、动态调用候选、对象成员状态写入、行位移后的关系保持，以及整份/按需两种生成模式和人工依据的 HTTP 持久化。
- `pnpm check:v3` 和 `pnpm check:v7` 通过，确认既有静态引用与 MR 增量复审行为未被本次改动破坏。
- 在独立端口 `4319`、临时 Git 仓库和临时数据目录中操作真实页面：选中 `submit` 后显示 9 条关系与 4 个相关文件；展开 `request` 后显示 14 条关系；进入未变更的 `src/transport.ts` 时显示固定源码片段，返回影响链时高亮状态保留。覆盖矩阵显示两处变更；保存其中一处“已核实”后矩阵显示已核实，而原有逐块审查状态仍为未阅读。解释卡的源码证据点击后定位到对应固定快照片段。
- 在已有 `app-justlend` 固定快照中对 `src/components/Connect/Entry.jsx` 的 `handleConnect` 做只读索引检查：两侧共索引 783 个 JS/TS 源码文件，得到 50 条静态关系和 7 个相关文件，耗时约 1.6 秒。
- 页面上先选“逐块按需”后，“重新生成导读”才由禁用变为可点击；已有阅读路线中的可分析 hunk 显示独立的“生成此块解释”按钮。未点击生成按钮，因此没有消耗真实 Codex 额度。

## 未验证与边界

- 未实际消耗 Codex 额度生成逐块解释；结构化结果由定向测试的假 Provider 和临时验收数据验证。
- 未连接真实 GitLab MR 验证描述读取；MR 证据摘录及固定版本约束通过本地数据与校验逻辑验证。
- 未执行桌面打包、真实钱包或其他被审查项目的测试。按仓库规则，未准备 push，未执行 `pnpm test`。
- 符号影响链首期只覆盖两次提交和 GitLab MR 的固定 Git 树；动态调用、回调、事件总线、依赖注入和运行时状态仅能作为候选或待确认信息。
- 逐块按需 UI 选择显式按钮触发；本机页面已确认按钮出现，完整生成与保存路径由假 Provider 的 HTTP 定向测试验证。
